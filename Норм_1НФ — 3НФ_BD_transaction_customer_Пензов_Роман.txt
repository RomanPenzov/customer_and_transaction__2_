1. Разбили две таблицы customer и transaction (это два листа из файла customer_and_transaction__2_.xlsx) 
на четыре таблицы: product, customer, address_full и transaction.
2. Для соответствия 1НФ (не должно быть дублирующих строк в каждой таблице;
	 		в каждой ячейке должно быть одно не составное значение ;
			в столбце должны быть данные одного типа).
 Проанализировав как лист файла-xls на вопрос зависимости значений полей одних от других
 и их сторогостью заполнения данными (например, есть product_id = 0 с почти хоатичным заполнением остальных полей),
 добавил в соответствующие таблицы новые два поля
 "new_product_id" и "address_id" (с помощью операции "сцепить" в Excel из нескольких полей).
 Цены пришлось оставить в таблице транзакций, так как при product_id = 0 их заваисимость от продукта хаотичная 
 (если возможно, то на будущее было бы правильно договориться с тем, кто предоставляет данные, о сторгости заполнения данными).

3. 2НФ - каждая таблица имеет свой ключ;
	 неключевые столбцы зависят только от ключа;
Table customer -  customer_id integer [primary key];
Table address_full - address_id varchar [primary key];
Table product - new_product_id varchar [primary key];
Table transaction - transaction_id integer [primary key];

Ранее в первоначальных таблицах не было зависимости от ключей

4. 3НФ - теперь в каждой из 4-х таблиц отсутствует транзитивная зависимость (между неключевыми столбцами).
(характеристики продукта, характеристики клиента, характеристики клиента, характеристики транзакции - 
эти различные сущности ранее не были разбиты по отдельным таблицам со своими ключами).

5. отдельно добавил в таблицы связи через внешние ключи.

6. Есть один нюанс. В первоначальном файле в листе transaction есть один единственный клиент с customer_id = 5034,
   а в листе customer такого клиента нет. Поэтому для актуальности данных этого клиента надо занес в таблицу customer как test.

7. при импорте типы даты загружал как тип даты.

8. хотелось некоторые значения полей исправить (например, в поле gender, и в других), но этого не было в задании,
  а возможно, что это нужно оставить для читоты эксперимента.